# Lock Screen Appearance
#
# Reserved for future lock-screen variants (per-host themes,
# alternative layouts from Hyprlock-Dots, etc.). Currently only one
# style ships; the option exists so a future variant can be added
# without another rename pass.
#
# See modules/hyprland/hyprlock-themed.conf for the current layout
# (adapted from Layout 17 of mahaveergurjar/Hyprlock-Dots — centred
# authenticate box with the hostname title).
