schema: spec-driven
created: 2026-09-16
status: stub
note: stub change tracking the user's checklist item "rice hyprland a
  bit more than before so it looks less shit" (noted as out-of-scope
  for the other in-flight changes). Tasks and specs intentionally TBD —
  scope is "what would 'better than current' actually mean" and the
  user has not yet defined that.
