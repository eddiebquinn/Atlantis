# Tasks (mostly TBD)

## 1. Scope

- [ ] 1.1 User defines what "rice" means concretely (color palette?
  font? widget layout? wallpaper? animations? combination?).
- [ ] 1.2 User defines per-host vs fleet-wide scope.
- [ ] 1.3 User confirms sequencing: after both `atlantis-26-05-declarative-compositor`
  and `atlantis-dendritic-layout` land and are stable-soaked.

## 2-N. Implementation

(Tasks 2 onward to be defined after §1 is settled. The implementation
will likely mirror the structure of `atlantis-26-05-declarative-compositor`:
branch from master, eval gate, per-host verification windows, MR
hygiene, stability soak.)
