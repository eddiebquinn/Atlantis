## Context

This change is a stub created to give the user's checklist item "rice
hyprland a bit more than before so it looks less shit" a home in
openspec. It carries no actual scope yet — the user has not defined
what "rice" means in concrete terms (color palette, fonts, widget
layout, animations, wallpaper, etc.) and the change cannot be planned
further until that is settled.

The other two in-flight openspec changes (`atlantis-26-05-declarative-compositor`,
`atlantis-dendritic-layout`) are explicitly out of scope here; the
rice change should land after both so it operates on the declarative
substrate rather than being undone by later config moves.
