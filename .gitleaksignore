# Atlantis gitleaks allowlist.
#
# Format: <file>:<line>  OR  <file>:<RuleID>:<hash>
#
# gitleaks reads this file from the repo root automatically; the meditek
# template's before_script clobbers any local .gitleaks.toml but does not
# touch this file.
#
# modules/home/git.nix:7 — public GPG signing-key fingerprint declared in
# the git signing config. It's a public identifier, not a credential, but
# gitleaks's default `generic-api-key` rule matches any 40-char uppercase
# hex string with entropy > 3.5 (the fingerprint scores 3.78).
modules/home/git.nix:7