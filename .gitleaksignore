# Atlantis gitleaks allowlist.
#
# gitleaks reads this file from the repo root regardless of the
# --config file's [extend] settings. Format: one fingerprint per line,
# obtained from a gitleaks run report (the `Fingerprint` field).
#
# This entry allowlists the public GPG signing-key fingerprint stored in
# modules/home/git.nix line 7. It's not a credential — it's a public
# identifier used to verify signed commits — but gitleaks's default
# `generic-api-key` rule pattern is broad enough to match the 40-char
# hex string anyway.
modules/home/git.nix:generic-api-key:*